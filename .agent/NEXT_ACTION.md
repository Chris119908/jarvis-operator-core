# Next Action

Close the next smallest remaining V1 gap with tests:

- wire configured provider selection into the runtime path
- keep provider switching minimal and backed by focused tests
- verify `mock`, `ollama`, and `openai_compatible` selection behavior
- update `.agent/STATE.json`, `.agent/SUMMARY.md`, and `.agent/NEXT_ACTION.md`
- record unresolved failures in `.agent/KNOWN_ISSUES.md` if any appear
