# Next Action

Continue replacing placeholder V1 end-to-end coverage one use case at a time:

- replace the next placeholder in `tests/e2e/` with a real test for another fixed V1 use case
- choose the smallest remaining use case that fits the current architecture
- implement only the minimum production code needed to make that one test meaningful
- update `.agent/STATE.json`, `.agent/SUMMARY.md`, and `.agent/NEXT_ACTION.md`
- record unresolved failures in `.agent/KNOWN_ISSUES.md` if any appear
